server:
  port: 8080

spring:
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      password: redis_wsxy
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/dormitory_autonomy_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: app_user
    password: app_password_2026
  mail:
    host: smtp.gmail.com
    port: 465
    username: ${MAIL_USERNAME:shawyu8829@gmail.com}
    password: ${MAIL_PASSWORD:ukcevjkvgnlsruoj}
    protocol: smtp
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: ${MAIL_SMTP_STARTTLS:false}
          ssl:
            enable: ${MAIL_SMTP_SSL:true}
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

app:
  mail:
    from: ${MAIL_FROM:${MAIL_USERNAME:shawyu8829@gmail.com}}
    from-name: ${MAIL_FROM_NAME:文旅导览智能体}

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.example.demo.pojo
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
